name: feishu-init
version: 1.0.0
author: openclaw
description: "检查飞书多维表格是否存在，缺失则自动创建"
license: MIT

permissions:
  - shell
  - filesystem

entryPoint:
  type: natural
  prompt: "参见 SKILL.md"

triggers:
  keywords: ["初始化飞书表格", "feishu init"]
